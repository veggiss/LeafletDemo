import React, { FC } from 'react';
import GeoJsonMap from './GeoJsonMap';

const App: FC = () => {
    return (
        <div className="container">
            <GeoJsonMap />
        </div>
    );
};
export default App;
